fast.png filter=lfs diff=lfs merge=lfs -text

# Website mascot artwork.
website/goblin.png filter=lfs diff=lfs merge=lfs -text

# Loongson / LoongArch64 wheels are served from html/wheels/ so the
# stride-align.com mirror can serve them for users who can't reach
# github.com. Each wheel is a multi-MB binary, tracked through git-lfs.
# (html/ is otherwise gitignored; html/wheels/ is the kept exception.)
html/wheels/**/*.whl filter=lfs diff=lfs merge=lfs -text
