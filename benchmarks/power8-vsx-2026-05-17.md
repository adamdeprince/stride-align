# Linux PowerPC64 VSX (Power8) Benchmark Notes - 2026-05-17

These results are Power8 VSX-only and should not be mixed with the x86,
Loongson, or macOS benchmark summaries. The source tree was built on a real
POWER8 (architected) guest from the same `adam.initial` git branch as the x86
baseline.

Raw artifact:

- `benchmarks/power8-vsx-2026-05-17.csv`: native `generic`, `swar`, and
  `linux_powerpc64_vsx` rows for `english`/`chinese`, `linear`/`affine`,
  widths 16 and 32, `1:1` and `1:many` shapes, with `--timing-split`.

Generated with:

```bash
PYTHONPATH=src .venv/bin/python -m stride_align.benchmark \
  --backends generic swar linux_powerpc64_vsx \
  --variants sw-farrar-score sw-score nw-score sw-path-info nw-path-info sw-cigar nw-cigar \
  --passes english chinese \
  --shapes all \
  --scoring-cases linear affine \
  --widths 16 32 \
  --iterations 15 \
  --warmups 3 \
  --timing-split \
  --format csv > benchmarks/power8-vsx-2026-05-17.csv
```

Build context:

- Host: real POWER8 silicon, PVR `004b 0201` (v2.1, "Murano"), 4.157 GHz clock,
  512 MHz timebase. KVM-virtualized as a single-core `pSeries` guest. The
  `/proc/cpuinfo` "emulated by qemu" string refers to the firmware/machine
  model that KVM-on-Power uses for guests, not to user-mode CPU emulation.
- OS: Ubuntu 20.04 ppc64le.
- Toolchain: IBM Advance Toolchain 15.0 (GCC 11.4.1) under `/opt/at15.0/bin`,
  Python 3.13.13 from a `miniforge` install, CMake 4.3.2 + Ninja from pip.
- Build: standard `scikit-build-core` flow with `CC=/opt/at15.0/bin/gcc
  CXX=/opt/at15.0/bin/g++`, `-O3` default. 4 GiB swap added because compiling
  the Farrar templates with `-O3` exceeds the 1 GiB RAM during instantiation.

Parasail status:

- Not built. Upstream parasail does not ship ppc64le wheels and a source build
  was not attempted in this pass. All ratios below are against `generic` on
  the same machine.

## Power8 VSX semantic notes that drove the implementation

- Lane numbering on Linux ppc64le is little-endian. The GCC altivec builtins
  largely hide BE register semantics, but `vec_sld` still exposes the BE
  Programming Interface Manual model: `vec_sld(zero, v, 14)` on the i16 lane
  shape is **not** "shift left by 1 lane". Cross-lane shifts use `vec_perm`
  with explicit LE-natural byte indices so the shift helpers match the
  SSE/NEON/LSX semantics one-to-one.
- An early bug in `shift_left_perm`: the low half of the permutation was a
  constant `16` instead of `16 + i`. This collapsed multi-byte inserted
  values (e.g. `set1(low_score)`) to byte 0 of the splat, dropping the upper
  bytes of the inserted scalar. Symptom: NW global lazy-F prefix carry
  produced wrong scores at widths 16/32 (correct at width 8 because byte 0
  was the whole value there). Fixed by using `16 + i` for the inserted
  indices.
- `vec_max` for `__vector signed long long` is a Power9 (ISA 3.0) instruction.
  On Power8 the `SimdOps<uint64_t, int64_t>::max` is synthesized via
  `vec_cmpgt + vec_sel` over the 2 lanes.
- `vec_sel` requires the per-width `__vector __bool char/short/int/long long`
  mask. The previous backend cast every comparison result to
  `__vector unsigned char`, which only happens to work for 8-bit lanes and
  was one source of the existing build break.
- `vec_cmpgt` for `signed long long` requires ISA 2.07, which is Power8, so
  the 64-bit width has real SIMD comparisons.
- Power8 has `vbpermq` (vector bit permute quadword). With control bytes
  `[127, 119, ..., 15, 7]` it collapses a per-byte 0xFF/0x00 mask into a
  16-bit LE-natural lane bitmap in one instruction. Profiling showed the
  trace-mask step is only ~4% of the masked-traceback runtime on 1024x1024
  English, so `SimdOps::trace_mask_eq/gt` uses a simple vector store + scalar
  fan-out instead. The `vbpermq` route is documented inline for a future
  micro-optimization pass.
- `vec_xl(0, uint64_t*)` overload resolution picked the 32-bit-lane variant
  under GCC 11.4. Explicit cast to `unsigned long long*` forces the right
  one. Same story for `vec_vsx_st` against `int64_t*`.

## Routing decisions that differ from x86_avx2 / Loongson

1. **Linear SW path / path-info / cigar stay on `profile_traceback`**. The
   shared `dispatch_linear_sw_score_first_masked_cigar_*` path was wired up
   first (mirroring AVX2 and LSX) but measured 0.46-0.48x of the scalar
   byte-table path on 1024x1024 English. Profiling pointed at the striped
   trace table's poor cache locality on the traceback walk, not at the mask
   extraction. Reverted to `profile_traceback` and kept the helper hooks
   (`trace_mask_*`, `mask_or`, `store_masked_cells`) in `SimdOps` so any
   future redesign can reuse them.
2. **Affine SW / NW path / path-info also stay on `profile_traceback`**.
   `dispatch_affine_striped_path_info` measured 0.57-0.60x for the same
   reason. Affine **CIGAR** stays on the SIMD score kernel +
   `profile_traceback::affine_cigar_with_score` because it skips the trace
   table entirely; that route still beats `_generic` by ~2x on `sw-cigar`
   and `nw-cigar` rows in this sweep.
3. **Linear SW / NW score**, **batch (1:many) score**, **affine score
   (compact byte)**, and **prepared score profiles** all route through the
   shared Farrar fixed kernel via the new `SimdOps`. These are the big wins.

## Overall results

| Backend | Rows | Geomean vs Generic | Median vs Generic | Best vs Generic | Worst vs Generic |
| --- | ---: | ---: | ---: | ---: | ---: |
| `linux_powerpc64_vsx` | 80 | 3.65x | 4.13x | 16.82x | 0.96x |
| `swar` | 80 | 0.77x | 0.98x | 1.60x | 0.41x |

VSX wins or ties on every one of the 80 rows in this sweep (worst is 0.96x
of generic — within noise). SWAR is a regression overall on Power8, mostly
because generic auto-vectorizes well under AT15 GCC 11.4 on the score-only
loops where SWAR's 64-bit packed lanes give it no extra throughput.

## Score-only vs path/CIGAR breakdown

| Group | Rows | Geomean | Median | Best | Worst |
| --- | ---: | ---: | ---: | ---: | ---: |
| Score-only (`sw-score`, `sw-farrar-score`, `nw-score`) | 48 | 6.61x | 6.75x | 16.82x | 2.82x |
| Path + CIGAR (`sw-path-info`, `nw-path-info`, `sw-cigar`, `nw-cigar`) | 32 | 1.50x | 1.14x | 4.20x | 0.96x |

The shape is the same as on Loongson and macOS: score-only is where VSX
clears generic by 5-15x, and path/CIGAR is dominated by trace-table traffic
where SIMD only helps the DP score component.

## Per-variant for `linux_powerpc64_vsx`

| Variant | Rows | Geomean | Median | Best | Worst |
| --- | ---: | ---: | ---: | ---: | ---: |
| `sw-farrar-score` | 16 | 7.75x | 7.07x | 16.82x | 4.02x |
| `sw-score` | 16 | 7.58x | 7.10x | 16.03x | 4.05x |
| `nw-score` | 16 | 4.91x | 5.12x | 8.51x | 2.82x |
| `nw-cigar` | 8 | 1.89x | 2.03x | 4.14x | 0.98x |
| `sw-cigar` | 8 | 1.88x | 2.00x | 4.20x | 0.96x |
| `sw-path-info` | 8 | 1.23x | 1.21x | 1.58x | 1.01x |
| `nw-path-info` | 8 | 1.17x | 1.14x | 1.52x | 0.96x |

`sw-cigar` and `nw-cigar` affine rows are the big surprise. They route
through the SIMD score kernel + `profile_traceback::affine_cigar_with_score`
(score-first scalar reverse-build), which dodges the striped trace table
entirely. The affine `sw-cigar` row maxes at 4.20x.

## Linear vs affine

| Backend | Scoring | Rows | Geomean | Median |
| --- | --- | ---: | ---: | ---: |
| `linux_powerpc64_vsx` | linear | 40 | 3.49x | 4.72x |
| `linux_powerpc64_vsx` | affine | 40 | 3.82x | 4.13x |
| `swar`                | linear | 40 | 0.91x | 1.00x |
| `swar`                | affine | 40 | 0.65x | 0.46x |

Affine is slightly ahead on geomean because the score kernels are doing more
arithmetic per cell and the SIMD throughput shows through.

## English vs Chinese

| Backend | Pass | Rows | Geomean | Median |
| --- | --- | ---: | ---: | ---: |
| `linux_powerpc64_vsx` | english | 40 | 3.58x | 4.07x |
| `linux_powerpc64_vsx` | chinese | 40 | 3.73x | 4.25x |

Roughly even. Chinese targets a wider character set, but the Farrar score
kernels are score-table-based, so token cardinality doesn't change the
inner-loop cost much.

## Best 5 rows

| Pass | Case | Shape | Variant | Width | Speedup |
| --- | --- | --- | --- | ---: | ---: |
| chinese | linear | 1:1   | sw-farrar-score | 16 | 16.82x |
| chinese | linear | 1:many| sw-farrar-score | 16 | 16.04x |
| chinese | linear | 1:many| sw-score        | 16 | 16.03x |
| english | linear | 1:1   | sw-farrar-score | 16 | 15.92x |
| english | linear | 1:many| sw-farrar-score | 16 | 15.25x |

## Worst 5 rows

| Pass | Case | Shape | Variant | Width | Speedup |
| --- | --- | --- | --- | ---: | ---: |
| english | linear | 1:1 | nw-path-info | 16 | 0.96x |
| chinese | linear | 1:1 | sw-cigar     | 16 | 0.96x |
| english | linear | 1:1 | sw-cigar     | 16 | 0.96x |
| chinese | linear | 1:1 | nw-path-info | 16 | 0.97x |
| english | linear | 1:1 | nw-cigar     | 32 | 0.98x |

All worst rows are short `1:1` workloads at width 16/32 where SIMD setup and
trace-table allocation dominate. No row falls below 0.96x of generic.

## Recommended Power8 next steps

1. Try the `vbpermq` trace-mask fast path on real hardware to see whether the
   masked SW/affine traceback becomes competitive once mask extraction is
   one instruction. The other component of the gap is trace-table layout,
   which `vbpermq` does not address.
2. Consider a Power8-specific linear SW trace redesign that keeps the byte
   trace table row-major (matching `profile_traceback`'s layout) but uses
   VSX for the score DP only. This mirrors the affine CIGAR route that
   already beats generic by 2-4x.
3. Add a Power8 `local_affine_score_exact_segment128/256_raw` mirroring the
   NEON `local_affine_score_exact_fill_i16_128/i32_256` helpers, if the
   1024-character query shape becomes a target. Score-only is already
   ~7-8x ahead of generic; the marginal win is small.
4. Re-bench on a multi-core / non-virtualized Power8 host. This run was on
   a single-core KVM guest; SMT throughput and shared L2/L3 behavior on a
   physical 8-core / SMT8 chip are not represented here.
5. Build parasail from source for ppc64le and re-run with a parasail column
   to mirror the format used by the x86 and macOS sweeps.
6. Investigate why SWAR loses to generic on Power8. AT15 GCC 11.4 appears
   to auto-vectorize the generic score loops well enough that the 64-bit
   packed SWAR lanes give no benefit, but this is worth confirming with
   an asm dump rather than just timing.
