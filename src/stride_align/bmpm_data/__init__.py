"""Vendored Apache Commons Codec BMPM rule files (GENERIC name-type, Apache 2.0).

Loaded at runtime by :mod:`stride_align._bmpm` via :mod:`importlib.resources`.
Each ``*.txt`` file in this package preserves its upstream ASF header.
"""
